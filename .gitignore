# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# runtime data store (generated at runtime, never checked in)
/data/

# typescript
*.tsbuildinfo
next-env.d.ts

# claude code worktrees (each is its own git checkout — never track as a submodule/embedded repo)
/.claude/worktrees/
